<UserControl x:Class="Naps2Paperless.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Einstellungen" FontSize="20" FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" MaxWidth="500" HorizontalAlignment="Left">
            <!-- NAPS2 Path -->
            <TextBlock Text="NAPS2 Pfad:" Margin="0,0,0,4"/>
            <DockPanel Margin="0,0,0,12">
                <Button Content="..." DockPanel.Dock="Right" Padding="8,4"
                        Command="{Binding BrowseNaps2Command}" Margin="6,0,0,0"/>
                <TextBox Text="{Binding Naps2Path, UpdateSourceTrigger=PropertyChanged}"/>
            </DockPanel>

            <!-- Profile Name -->
            <TextBlock Text="Profilname:" Margin="0,0,0,4"/>
            <TextBox Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12"/>

            <!-- API Base URL -->
            <TextBlock Text="API Basis-URL:" Margin="0,0,0,4"/>
            <TextBox Text="{Binding ApiBaseUrl, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12"/>

            <!-- API Endpoint -->
            <TextBlock Text="API Endpunkt:" Margin="0,0,0,4"/>
            <TextBox Text="{Binding ApiEndpoint, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12"/>

            <!-- API Token -->
            <TextBlock Text="API Token:" Margin="0,0,0,4"/>
            <PasswordBox x:Name="ApiTokenBox" Margin="0,0,0,12"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Speichern &amp; Zurueck" Command="{Binding SaveCommand}"
                    Padding="16,8" FontSize="14" Margin="0,0,8,0"/>
            <Button Content="Zurueck" Command="{Binding BackCommand}"
                    Padding="16,8" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
